// Generated by LiveScript 1.6.0
(function(){
  var z, l, R, chalk, util, showOb, SI, guardjs, sim, traverse, prettyError, noop, utilInspectCustom, x$, c, moduleName, unfinished, guard;
  z = console.log;
  l = console.log;
  R = require("ramda");
  chalk = require("chalk");
  util = require("util");
  showOb = require("json-stringify-pretty-compact");
  SI = require("seamless-immutable");
  guardjs = require("guard-js");
  sim = require("seamless-immutable-mergers");
  traverse = require("traverse");
  prettyError = require("pretty-error");
  noop = function(){};
  if (typeof window === "undefined" && typeof module === "object") {
    util = require("util");
    utilInspectCustom = util.inspect.custom;
    noop[utilInspectCustom] = function(){
      return this[utilInspectCustom];
    };
  } else {
    utilInspectCustom = Symbol("spam");
  }
  x$ = c = {};
  x$.ok = chalk.green.bold;
  x$.er = chalk.hex("FF0000");
  x$.warn = chalk.hex("FFFFCD");
  moduleName = "valleydate";
  unfinished = function(name){
    return function(){
      l(" <| " + name + " |>");
    };
  };
  guard = function(whn, fn){
    return guardjs().when(whn, fn);
  };
  module.exports = {
    z: z,
    l: l,
    chalk: chalk,
    guard: guard,
    guardjs: guardjs,
    unfinished: unfinished,
    SI: SI,
    j: function(j){
      return console.log(showOb(j));
    },
    R: R,
    immutable: SI["static"],
    sim: sim,
    utilInspectCustom: utilInspectCustom,
    noop: noop,
    moduleName: moduleName,
    traverse: traverse,
    prettyError: prettyError
  };
}).call(this);
